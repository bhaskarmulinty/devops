pipeline {
    agent any

    stages {
        stage('Build') {
            steps {
                echo 'Checking Terraform Version '
               sh ('terraform version')
            }
        }
       
    }
}
